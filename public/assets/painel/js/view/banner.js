$( function(){		//função ao alterar	$(document).on('change', 'select[name="tipo"]', function(){		bannerInit($(this));	});	$('select[name="tipo"]').trigger('change');	});/** * start */function bannerInit(button){	var width = button.find("option:selected").data("width");	var height = button.find("option:selected").data("height");	var targetTxt = $('#uploadInfo');	var targetCss = $('.uploadClick');		if( width == undefined )	{		targetTxt.hide();		targetCss.hide();			} else {				targetTxt.show().find("b").text(width + "x" + height + "px");		if( width == 2000 ) width = (width / 2);		if( width == 2000 ) height = (height / 2);		targetCss.css("width", width).css("max-width", width).css("height", height).show();			}		}