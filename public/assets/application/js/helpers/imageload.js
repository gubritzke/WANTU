function imageLoadInit(){	//selecionar imagens que devem ser substituidas	var images = $("img[data-src]");		//definir o quanto ja foi rolado da página	var screen_top = parseInt($('body').scrollTop());		//loop nas imagens	images.each( function(i, e){				//definir distancia do elemento até o topo		var element_top = parseInt($(e).offset().top) + screen_top - 1000;				//definir nome da imagem		var img = $(e).attr('src');		var img = img.substring(img.lastIndexOf('/') + 1); 	    		//verificar se é a imagem de loading		var cond1 = (img == 'loading.gif');		var cond2 = (screen_top >= element_top);		if( cond1 && cond2 )		{			//console.log(i);			//console.log("screen: " + screen_top);			//console.log("element: " + element_top);			$(this).attr('src', $(this).attr('data-src'));		}            });}$(window).load( function(){		//init	imageLoadInit();		//executar ao rolar a tela	$('body').scroll( function() {		imageLoadInit();	});	});