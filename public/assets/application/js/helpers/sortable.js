$( function(){		//content	var content = $('.sortable');		//loop em todos os sortables	$.each(content, function(i, e){				var item = $(e).data('item');				$(e).sortable({			deactivate: function(event,ui){ 				var subcontent = $(e).find(item);								var data = [];								$.each(subcontent, function(index, element){					data[index] = $(element).data('id');				});								$.ajax({					type: "POST",					data: {method:'reorder', order:data}				});							}		});			});});