/** * função ao alterar o status */function statusChange(){	var form = $(this);	var status = form.find("option:selected").val();	var rastreio = form.find("input[name='frete_rastreio']").val();	var count = (rastreio == "") ? 0 : $("input[value='" + rastreio + "']").length;		//status cancelado	if( status == "6" )	{		return confirm("Deseja realmente cancelar? Esse processo não pode ser revertido!");	}		//status finalizado	if( status == "5" )	{		return confirm("Deseja realmente finalizar? Esse processo não pode ser revertido!");	}		//status produto em trânsito	if( (status == "4") && (count > 1) )	{		return confirm("Essa alteração de status afetará os outros itens com o mesmo código de rastreio. Deseja realmente continuar?");	}		return true;}/** * função ao fechar o box */function closeBox(){	location.reload();}$( function(){		//função ao alterar o status	$(document).on('submit', '.form-status', statusChange);		//função ao fechar o box	$(document).on('click', '.fancybox-close', closeBox);	});